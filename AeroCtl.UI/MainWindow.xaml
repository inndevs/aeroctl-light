<Window x:Class="AeroCtl.UI.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:AeroCtl.UI"
        mc:Ignorable="d"
        DataContext="{Binding RelativeSource={RelativeSource Self}}"
        Title="AERO Controls" Height="450" Width="800" Name="this">
    <ScrollViewer>
        <StackPanel>
            <StackPanel.Resources >
                <Style TargetType="GroupBox" >
                    <Setter Property="Margin" Value="5,5,5,0" />
                </Style>
            </StackPanel.Resources>
            <GroupBox Header="Info">
                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto" />
                        <RowDefinition Height="Auto" />
                        <RowDefinition Height="Auto" />
                        <RowDefinition Height="Auto" />
                        <RowDefinition Height="Auto" />
                        <RowDefinition Height="Auto" />
                    </Grid.RowDefinitions>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="Auto" />
                        <ColumnDefinition Width="*" />
                    </Grid.ColumnDefinitions>

                    <TextBlock Grid.Row="0" Grid.Column="0" Margin="2">Model / Base board</TextBlock>
                    <TextBox Grid.Row="0" Grid.Column="1" IsReadOnly="True" Text="{Binding ElementName=this, Path=Aero.BaseBoard, Mode=OneWay}" />

                    <TextBlock Grid.Row="1" Grid.Column="0" Margin="2">Serial Number</TextBlock>
                    <TextBox Grid.Row="1" Grid.Column="1" IsReadOnly="True" Text="{Binding ElementName=this, Path=Aero.SerialNumber, Mode=OneWay}" />

                    <TextBlock Grid.Row="2" Grid.Column="0" Margin="2">BIOS Version(s)</TextBlock>
                    <TextBox Grid.Row="2" Grid.Column="1" IsReadOnly="True" Text="{Binding ElementName=this, Path=Aero.BiosVersion, Mode=OneWay}" />

                    <TextBlock Grid.Row="3" Grid.Column="0" Margin="2">Keyboard firmware version</TextBlock>
                    <TextBox Grid.Row="3" Grid.Column="1" IsReadOnly="True" Text="{Binding ElementName=this, Path=Aero.KeyboardFWVersion, Mode=OneWay}" />

                    <TextBlock Grid.Row="4" Grid.Column="0" Margin="2">CPU Temperature</TextBlock>
                    <StackPanel Grid.Row="4" Grid.Column="1" Orientation="Horizontal">
                        <TextBlock Text="{Binding ElementName=this, Path=Aero.CpuTemperature, Mode=OneWay}" />
                        <TextBlock Margin="5 0 0 0">°C</TextBlock>
                    </StackPanel>

                    <TextBlock Grid.Row="5" Grid.Column="0" Margin="2">Wifi Enabled</TextBlock>
                    <CheckBox Grid.Row="5" Grid.Column="1" IsChecked="{Binding ElementName=this, Path=Aero.WifiEnabled}" />
                </Grid>
            </GroupBox>

            <GroupBox Header="Display">
                <StackPanel>
                    <Label Target="{Binding ElementName=screenBrightness}">
                        <StackPanel Orientation="Horizontal">
                            <TextBlock Margin="0 0 5 0">Screen brightness:</TextBlock>
                            <TextBlock Text="{Binding ElementName=this, Path=Aero.ScreenBrightness, Mode=OneWay}" />
                        </StackPanel>
                    </Label>
                    <Slider Name="screenBrightness" Minimum="0" Maximum="100" IsSnapToTickEnabled="True" Value="{Binding ElementName=this, Path=Aero.ScreenBrightness}" />
                </StackPanel>
            </GroupBox>

            <GroupBox Header="Battery">
                <StackPanel>
                    <CheckBox IsChecked="{Binding ElementName=this, Path=Aero.ChargeStopEnabled}">Enable charge stop</CheckBox>

                    <Label Target="{Binding ElementName=chargeStop}">
                        <StackPanel Orientation="Horizontal">
                            <TextBlock Margin="0 0 5 0">Charge stop:</TextBlock>
                            <TextBlock Text="{Binding ElementName=this, Path=Aero.ChargeStop, Mode=OneWay}" />
                            <TextBlock Margin="5 0 0 0">%</TextBlock>
                        </StackPanel>
                    </Label>
                    <Slider Name="chargeStop" Minimum="1" Maximum="100" IsSnapToTickEnabled="True" Value="{Binding ElementName=this, Path=Aero.ChargeStop}" />
                </StackPanel>
            </GroupBox>
            
            <GroupBox Header="Fans">
                <StackPanel>
                    <StackPanel Orientation="Horizontal">
                        <TextBlock Margin="0 0 5 0">Fan 1 RPM:</TextBlock>
                        <TextBlock Text="{Binding ElementName=this, Path=Aero.FanRpm1, Mode=OneWay}"></TextBlock>
                    </StackPanel>
                    <StackPanel Orientation="Horizontal">
                        <TextBlock Margin="0 0 5 0">Fan 2 RPM:</TextBlock>
                        <TextBlock Text="{Binding ElementName=this, Path=Aero.FanRpm2, Mode=OneWay}"></TextBlock>
                    </StackPanel>

                    <StackPanel Orientation="Horizontal">
                        <Button Name="fanProfileQuiet" Click="FanProfileQuiet_OnClick">Quiet</Button>
                        <Button Name="fanProfileNormal" Click="FanProfileNormal_OnClick">Normal</Button>
                        <Button Name="fanProfileGaming" Click="FanProfileGaming_OnClick">Gaming</Button>
                        <Button Name="fanProfileAuto" Click="FanProfileAuto_OnClick">Custom (Auto)</Button>
                        <Button Name="fanProfileFixed" Click="FanProfileFixed_OnClick">Custom (Fixed)</Button>
                    </StackPanel>

                    <CheckBox IsChecked="{Binding ElementName=this, Path=Aero.AutoFan}">Auto Fan</CheckBox>
                    <CheckBox IsChecked="{Binding ElementName=this, Path=Aero.FixedFan}">Fixed Fan</CheckBox>
                    <CheckBox IsChecked="{Binding ElementName=this, Path=Aero.StepFan}">Step Fan</CheckBox>
                    <CheckBox IsChecked="{Binding ElementName=this, Path=Aero.MaxFan}">Max Fan</CheckBox>
                    <CheckBox IsChecked="{Binding ElementName=this, Path=Aero.NvThermalTarget}">NvThermalTarget (?)</CheckBox>

                    <Label Target="{Binding ElementName=fanAdjust}">Fan Adjust</Label>
                    <Slider Name="fanAdjust" Minimum="0" Maximum="229" IsSnapToTickEnabled="True" Value="{Binding ElementName=this, Path=Aero.FanAdjust}" />

                    <Label Target="{Binding ElementName=fixedFanSpeed}">Fixed Fan Speed</Label>
                    <Slider Name="fixedFanSpeed" Minimum="0" Maximum="229" IsSnapToTickEnabled="True" Value="{Binding ElementName=this, Path=Aero.FixedFanSpeed}" />

                    <Button Click="ButtonBase_OnClick">Reload</Button>
                    <DataGrid Name="curveGrid" Height="320">

                    </DataGrid>
                </StackPanel>
            </GroupBox>
        </StackPanel>
    </ScrollViewer>
</Window>
